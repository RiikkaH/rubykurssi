<table class="table table-hover">
  <thead>
    <tr>
      <th></th>
      <th>average</th>
   </tr>
  </thead>
  <tbody>
    <% beers_or_breweries.each do |b| %>
      <tr>
        <td><%= b %></td><td><%= round(b.average_rating) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
